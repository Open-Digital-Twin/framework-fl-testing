apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fl-framework-storageclaim-experiments
  namespace: fschwanck
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Gi
  storageClassName: rook-cephfs



